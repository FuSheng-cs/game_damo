# Tasks

- [x] Task 1: Clean up old lore and endpoints
  - [x] Delete `src/views/TrueEndingView.vue` and remove it from `src/router/index.ts`.
  - [x] Remove `previousSummary` and complex loop logic from `src/store/gameStore.ts`.
  - [x] Update `src/modules/AchievementTracker.ts` to revert to the three basic endings (死亡, 消失, 相识).
- [x] Task 2: Implement Affection System & Dynamic Rounds
  - [x] Add `affection` state (starts at 0) to `gameStore.ts`.
  - [x] Update the message parsing logic in `gameStore.ts` to look for a `[好感度+5]` tag. If found, add 5 to `affection` and add 1 to `roundCount`.
  - [x] Update `src/views/GameView.vue` to display the affection progress bar and animate the changes.
- [x] Task 3: Rewrite the LLM Prompt (`LLMService.ts`)
  - [x] Write the new character persona: Orphan, night photographer, extremely nihilistic (Fire Punch core), mysterious, shattered charm (Goodbye Eri core), suicidal but captivates the player at first sight.
  - [x] Define the strict rules for the AI to output `[好感度+5]` only when the user's message is truly touching or interesting.
  - [x] Redefine the 3 endings: `[结局:死亡]`, `[结局:消失]`, `[结局:相识]`.
- [x] Task 4: Create the "After Story" Chat Page
  - [x] Create `src/views/ChatAfterStoryView.vue` mimicking a WeChat UI.
  - [x] Add the route to `src/router/index.ts`.
  - [x] In `GameView.vue`, if the ending is `end_acquaintance`, change the button to "添加好友" or similar, linking to the new chat page.
  - [x] Implement a simple free-chat prompt for the After Story in `LLMService.ts` so the player can actually chat with her.
- [x] Task 5: Update Documentation
  - [x] Rewrite `docs/storyline_and_lore.md` to reflect the new lore.
  - [x] Rewrite `docs/prompts_and_settings.md` to reflect the new prompts and mechanics.
  - [x] Update `README.md` game intro.